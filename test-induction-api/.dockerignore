# Build artifacts and IDE/sbt state — never needed in the image build context.
target/
project/target/
project/project/
.bloop/
.metals/
project/.bloop/
project/metals.sbt
.idea/
*.log

# UI build artifacts (the image builds the UI fresh from ui/ sources).
ui/node_modules/
ui/dist/
src/main/resources/web/
